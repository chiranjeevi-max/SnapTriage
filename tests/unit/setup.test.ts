/**
 * @module tests/unit/setup
 *
 * Smoke test to verify that the Vitest test runner is configured correctly.
 */
import { describe, it, expect } from "vitest";

describe("setup", () => {
  it("works", () => {
    expect(true).toBe(true);
  });
});
