1. Add `memo` to `IssueRow` in `src/features/inbox/components/issue-list.tsx` and fix its props.
2. Use `useCallback` for `handleSelect` in `src/features/inbox/components/inbox-view.tsx`.
3. Check `package.json` for tests and linting.
