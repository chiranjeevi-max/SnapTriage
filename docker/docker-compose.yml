services:
  app:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "3000:3000"
    env_file:
      - ../.env.local
    volumes:
      - snaptriage-data:/app/data
    restart: unless-stopped

volumes:
  snaptriage-data:
